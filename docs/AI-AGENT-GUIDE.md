# 🤖 คู่มือการใช้งานเอกสารกับ AI Agent

> **สำหรับ AI Agent ที่จะทำงานกับโปรเจค Smart Agriculture Monitoring System**  
> **กฎเหล็ก: ห้ามคิดเอง ทำตามเอกสารเท่านั้น**

---

## 📋 สารบัญ

1. [Prompt Templates พื้นฐาน](#-prompt-templates-พื้นฐาน)
2. [ตัวอย่างการใช้งานจริง](#-ตัวอย่างการใช้งานจริง)
3. [กฎเหล็กที่ต้องจำ](#-กฎเหล็กที่ต้องจำ)
4. [Quick Reference Table](#-quick-reference-table)
5. [Troubleshooting](#-troubleshooting)

---

## 📝 Prompt Templates พื้นฐาน

### 1️⃣ เมื่อเริ่มทำงานใหม่ทุกครั้ง (Initialization)

```
@docs ช่วยอ่านเอกสารในโฟลเดอร์ docs ทั้งหมดเพื่อทำความเข้าใจโปรเจค 
Smart Agriculture Monitoring System โดยเฉพาะ:

- @docs/00-roadmap-overview.md (ภาพรวมโปรเจค)
- @docs/02-requirement-grouping-priority.md (Priority Level)
- @docs/06-erd-database-design.md (Database Structure)
- @docs/07-data-model-api-design.md (API Design)

กฎสำคัญ: ห้ามคิดเอง ทำตามเอกสารเท่านั้น
หากไม่แน่ใจในส่วนใด ให้ถามก่อนลงมือทำ
```

**📌 เมื่อไหร่ใช้:**
- เริ่มต้นทำงานครั้งแรก
- เปิด session ใหม่
- มีคนใหม่เข้ามาในทีม

---

### 2️⃣ เมื่อต้องการสร้าง Feature ใหม่ (Feature Development)

```
ช่วยสร้าง [ชื่อ Feature] โดยอ้างอิงจาก:

1. Use Case: @docs/04-use-case-diagram.md#[UC0X: ชื่อ Use Case]
2. Data Flow: @docs/05-data-flow-diagram.md#[Process ที่เกี่ยวข้อง]
3. Data Model: @docs/06-erd-database-design.md#[Entity ที่เกี่ยวข้อง]
4. API Endpoint: @docs/07-data-model-api-design.md#[API ที่เกี่ยวข้อง]

ข้อกำหนด:
- ห้ามเพิ่มฟีเจอร์ที่ไม่มีในเอกสาร
- ต้องตรวจสอบ Priority Level ใน @docs/02-requirement-grouping-priority.md
- ต้องเคารพ Access Control ที่กำหนดไว้
```

**📌 เมื่อไหร่ใช้:**
- สร้าง Component ใหม่
- เพิ่ม Page ใหม่
- Implement API ใหม่

---

### 3️⃣ เมื่อต้องการตรวจสอบความถูกต้อง (Validation)

```
ตรวจสอบโค้ด/Feature ที่เพิ่งสร้างว่าสอดคล้องกับเอกสารหรือไม่:

ตรวจสอบ:
1. Architecture: @docs/03-system-scope-architecture.md
2. Use Case: @docs/04-use-case-diagram.md
3. Data Flow: @docs/05-data-flow-diagram.md
4. Database Design: @docs/06-erd-database-design.md
5. API Design: @docs/07-data-model-api-design.md

กรุณาบอกส่วนที่:
✅ ตรงตามเอกสาร
⚠️ ต้องปรับปรุง
❌ ไม่ตรงตามเอกสาร
```

**📌 เมื่อไหร่ใช้:**
- หลังสร้าง Feature เสร็จ
- ก่อน Commit Code
- Review Code

---

### 4️⃣ เมื่อต้องการเช็ค Priority Level (Priority Check)

```
ตรวจสอบใน @docs/02-requirement-grouping-priority.md ว่า 
[Feature/Component Name] อยู่ใน Priority Level ไหน?

- ถ้าอยู่ใน Level 1 (Core) → สร้างได้เต็มรูปแบบ
- ถ้าอยู่ใน Level 2 (Demo Value) → สร้างแบบง่าย หรือ Mock
- ถ้าอยู่ใน Level 3 (Future) → ห้ามสร้าง บอกว่าเป็น Future Phase

อธิบายเหตุผลด้วย
```

**📌 เมื่อไหร่ใช้:**
- ก่อนเริ่มทำงานใหม่
- มีการขอเพิ่มฟีเจอร์
- ไม่แน่ใจว่าควรทำหรือไม่

---

### 5️⃣ เมื่อต้องการดู Access Control (Permission Check)

```
ตรวจสอบสิทธิ์การใช้งานจาก 
@docs/07-data-model-api-design.md#role-based-access-control

คำถาม:
1. [Feature/Endpoint นี้] ใช้ได้กับ Role ไหนบ้าง?
2. ต้องตรวจสอบสิทธิ์อย่างไร?
3. ต้อง return error อะไรถ้าไม่มีสิทธิ์?
```

**📌 เมื่อไหร่ใช้:**
- สร้าง Protected Route/Page
- Implement API Endpoint
- ตรวจสอบ Authorization Logic

---

### 6️⃣ เมื่อต้องการสร้าง Database Query (Database Query)

```
ช่วยสร้าง Query สำหรับ [วัตถุประสงค์] โดยอ้างอิงจาก:

1. ERD: @docs/06-erd-database-design.md
2. Relationships: ดูความสัมพันธ์ระหว่าง Entity
3. Index: ตรวจสอบว่ามี Index ที่เหมาะสมหรือไม่

ข้อกำหนด:
- ต้องเคารพ Relationship ที่กำหนดไว้
- ห้ามเพิ่ม Field ที่ไม่มีใน ERD
- ใช้ตัวแปรตามหลัก naming convention
```

**📌 เมื่อไหร่ใช้:**
- สร้าง Database Query
- Optimize Performance
- Debug Query Issues

---

### 7️⃣ เมื่อต้องการสร้าง API Endpoint (API Implementation)

```
ช่วยสร้าง API Endpoint สำหรับ [วัตถุประสงค์] ตาม:

@docs/07-data-model-api-design.md#api-endpoints-summary

รายละเอียด:
- Method: [GET/POST/PUT/DELETE]
- Path: [/api/...]
- Request Body/Query: [ระบุ]
- Response Format: JSON
- Access Control: [ระบุ Role]
- Error Handling: ตาม API Design Principles

ห้ามสร้าง Endpoint ที่ไม่มีในเอกสาร
```

**📌 เมื่อไหร่ใช้:**
- พัฒนา Backend API
- เชื่อมต่อ Frontend กับ Backend
- Test API

---

### 8️⃣ เมื่อต้องการเข้าใจ Data Flow (Data Flow Understanding)

```
อธิบาย Data Flow ของ [Feature/Process] โดยอ้างอิงจาก:

@docs/05-data-flow-diagram.md

คำถาม:
1. ข้อมูลมาจากไหน? (External Entity/Input)
2. ผ่าน Process ไหนบ้าง? (P1-P6)
3. ถูกเก็บที่ Data Store ไหน? (D1-D4)
4. ถูกส่งไปที่ไหน? (Output/User)
```

**📌 เมื่อไหร่ใช้:**
- เข้าใจ Business Logic
- Debug ปัญหา
- ออกแบบ Feature ใหม่

---

## 💡 ตัวอย่างการใช้งานจริง

### ✅ ตัวอย่างที่ 1: สร้าง Login Page

```
@docs ช่วยสร้าง Login Page สำหรับ React + TypeScript ตาม:

1. Use Case: @docs/04-use-case-diagram.md#uc01-login
2. User Model: @docs/06-erd-database-design.md#entity-2-user
3. API: @docs/07-data-model-api-design.md#authentication

ความต้องการ:
- Form รับ username และ password
- เรียก API: POST /api/auth/login
- รองรับ 3 roles: USER, MANAGER, SUPER_USER
- Redirect ตาม role หลัง login สำเร็จ
- แสดง error message ถ้า login ไม่สำเร็จ
- ใช้ password_hash (ห้ามเก็บ plain text)

UI Requirements:
- Responsive design
- รองรับ Light/Dark mode
- ใช้ shadcn/ui components ตาม project structure
```

**✨ ผลลัพธ์ที่คาดหวัง:**
- Login component ที่สมบูรณ์
- Type-safe TypeScript
- Integration กับ AuthContext
- Error handling ครบถ้วน

---

### ✅ ตัวอย่างที่ 2: สร้าง Dashboard Page

```
@docs สร้าง Dashboard Page ตามสถาปัตยกรรม Station-first:

อ้างอิงจาก:
1. Architecture: @docs/03-system-scope-architecture.md#station-first-architecture
2. Use Case: @docs/04-use-case-diagram.md#uc02-view-dashboard
3. API: @docs/07-data-model-api-design.md#stations

Features ที่ต้องมี:
- Map แสดงตำแหน่ง Station (ใช้ latitude, longitude)
- Station markers มี 3 สถานะ: Normal/Warning/Critical
- คลิก marker แสดง Station Overview
- Summary cards แสดง:
  * จำนวน Station ทั้งหมด
  * จำนวน Active Alerts
  * จำนวน Sensors
- Filter ตาม province
- Access: Manager, User, Super User (ทุกคน)

ห้ามเพิ่มฟีเจอร์ที่ไม่มีใน Use Case
```

**✨ ผลลัพธ์ที่คาดหวัง:**
- Dashboard layout สมบูรณ์
- Map integration
- API integration
- Role-based access ถูกต้อง

---

### ✅ ตัวอย่างที่ 3: สร้าง Sensor Data API

```
@docs สร้าง API Endpoint สำหรับ Sensor Data:

อ้างอิงจาก:
1. Entity: @docs/06-erd-database-design.md#entity-5-sensordata
2. API Spec: @docs/07-data-model-api-design.md#sensor-data

Endpoints ที่ต้องสร้าง:

1. POST /api/sensors/{id}/data
   - รับค่าจาก Sensor
   - Validate sensor_id มีอยู่จริง
   - บันทึกลง SENSOR_DATA
   - Trigger threshold check
   - สร้าง Alert ถ้าเกิน threshold

2. GET /api/sensors/{id}/data?from=&to=
   - Query time-series data
   - ใช้ Index (sensor_id, recorded_at)
   - Return JSON สำหรับทำกราฟ
   - รองรับ pagination

Access Control:
- POST: System/Manager only
- GET: All authenticated users

ต้องมี Error Handling ครบถ้วน
```

**✨ ผลลัพธ์ที่คาดหวัง:**
- API endpoints ที่ทำงานสมบูรณ์
- Business logic ถูกต้อง
- Performance optimization
- Error handling

---

### ✅ ตัวอย่างที่ 4: Validate Code ที่เขียนแล้ว

```
@docs ตรวจสอบโค้ดที่เพิ่งสร้าง StationDetail Component ว่าถูกต้องหรือไม่:

ตรวจสอบตาม:
1. Use Case: @docs/04-use-case-diagram.md#uc04-view-station-detail
2. Data Model: @docs/06-erd-database-design.md#entity-1-station
3. API: @docs/07-data-model-api-design.md#stations

รายการตรวจสอบ:
✓ แสดงข้อมูล Station ครบถ้วน (station_name, province, lat, long, status)
✓ แสดงรายการ Sensor ภายใน Station
✓ แสดง Alert ของ Station นี้
✓ Include View Sensor Data (drill-down)
✓ Access Control ถูกต้อง (Manager, User, Super User)
✓ ไม่มีฟีเจอร์ที่ไม่ได้ระบุใน Use Case

บอกส่วนที่ไม่ตรงตามเอกสาร พร้อมแนะนำวิธีแก้ไข
```

**✨ ผลลัพธ์ที่คาดหวัง:**
- รายงานการตรวจสอบ
- ส่วนที่ต้องปรับปรุง
- แนะนำวิธีแก้ไข

---

### ✅ ตัวอย่างที่ 5: เช็ค Priority ก่อนทำงาน

```
@docs ก่อนที่จะสร้าง "Weather Forecast Feature" 
ช่วยตรวจสอบว่าควรทำหรือไม่:

ตรวจสอบใน:
@docs/02-requirement-grouping-priority.md

คำถาม:
1. Feature นี้อยู่ใน Priority Level ไหน?
2. ถ้าอยู่ใน Level 3 (Future) → ห้ามทำ
3. ถ้าไม่มีในรายการ → ห้ามทำ
4. มี Use Case รองรับหรือไม่?

ตอบพร้อมเหตุผลชัดเจน
```

**✨ ผลลัพธ์ที่คาดหวัง:**
- คำตอบชัดเจน: ควรทำหรือไม่
- เหตุผลอ้างอิงเอกสาร
- ข้อเสนอแนะ Alternative (ถ้ามี)

---

### ✅ ตัวอย่างที่ 6: สร้าง Database Migration

```
@docs สร้าง Database Migration Script สำหรับโปรเจคนี้:

อ้างอิงจาก:
@docs/06-erd-database-design.md

สร้าง Migration สำหรับ:
1. All 7 Entities (Role, User, Station, Sensor, SensorData, Alert, Threshold)
2. Foreign Key Constraints ครบถ้วน
3. Index ที่จำเป็น (โดยเฉพาะ time-series)
4. Default values ตาม specification
5. Enum constraints ที่กำหนดไว้

รูปแบบ: SQL Script สำหรับ [PostgreSQL/MySQL/SQLite]
ห้ามเพิ่ม Field ที่ไม่มีใน ERD
```

**✨ ผลลัพธ์ที่คาดหวัง:**
- SQL Migration Script สมบูรณ์
- Foreign Keys ถูกต้อง
- Index ครบถ้วน
- พร้อม Execute

---

## 🚨 กฎเหล็กที่ต้องจำ

### ⚠️ ห้ามทำอย่างเด็ดขาด (NEVER DO)

| ❌ ห้ามทำ | เหตุผล |
|----------|--------|
| **คิดเอง** | ต้องทำตามเอกสารเท่านั้น |
| **เพิ่มฟีเจอร์** ที่ไม่มีใน Use Case | ทำให้ scope บานปลาย |
| **เปลี่ยน Data Model** ที่กำหนดไว้ใน ERD | ทำลายความสอดคล้อง |
| **เปลี่ยน API Endpoint** ที่ระบุไว้ | ทำให้ Frontend/Backend ไม่ตรงกัน |
| **สร้าง Entity/Field** ใหม่โดยไม่มีใน ERD | ทำลายโครงสร้าง Database |
| **ข้าม Priority Level 3** | เป็น Future Phase ห้ามทำใน Demo |
| **ไม่ตรวจสอบ Access Control** | มีปัญหา Security |
| **ใช้ plain password** | ต้องใช้ hash เท่านั้น |

---

### ✅ ต้องทำทุกครั้ง (ALWAYS DO)

| ✅ ต้องทำ | เหตุผล |
|----------|--------|
| **อ่านเอกสารก่อน** | เพื่อเข้าใจบริบท |
| **เช็ค Priority Level** | ก่อนเริ่มทำงานทุกครั้ง |
| **อ้างอิง Use Case** | ทุก Feature ต้องมี Use Case รองรับ |
| **ตรวจสอบ ERD** | ก่อนสร้าง/แก้ไข Database |
| **เช็ค Access Control** | ก่อนสร้าง Protected Resource |
| **ถามเมื่อไม่แน่ใจ** | ดีกว่าทำผิด |
| **Validate Input** | ตาม Constraints ใน Data Model |
| **Handle Error** | ครบทุก Case |

---

### 🎯 หลักการทำงาน (Work Principles)

#### 1. ลำดับการทำงาน (Work Flow)

```
1. อ่านเอกสารที่เกี่ยวข้อง
   ↓
2. เช็ค Priority Level
   ↓
3. ตรวจสอบ Use Case รองรับหรือไม่
   ↓
4. ดู Data Model / API Design
   ↓
5. เขียนโค้ด / สร้าง Feature
   ↓
6. Validate กับเอกสาร
   ↓
7. Test และ Error Handling
```

#### 2. คำถามก่อนเริ่มงาน (Pre-Work Questions)

ก่อนเริ่มทำงานใด ๆ ถามตัวเองเสมอ:

- [ ] Use Case นี้มีในเอกสารหรือไม่?
- [ ] Priority Level เท่าไหร่? (1, 2, หรือ 3)
- [ ] Entity/Data Model รองรับหรือไม่?
- [ ] API Endpoint มีระบุไว้หรือไม่?
- [ ] Access Control เป็นอย่างไร?
- [ ] มี Constraint อะไรบ้าง?

#### 3. เมื่อไม่แน่ใจ (When in Doubt)

```
ถาม > สมมติ
```

**ตัวอย่าง:**
```
ไม่แน่ใจว่าควรสร้าง Feature X หรือไม่

❌ ไม่ควรทำ: สมมติว่าน่าจะต้องมี แล้วสร้างไป
✅ ควรทำ: ถามว่า "Feature X อยู่ใน Use Case ไหน? Priority Level ไหน?"
```

---

## 📊 Quick Reference Table

### 🗂️ เอกสารอ้างอิงหลัก

| ต้องการข้อมูลเรื่อง | ดูเอกสาร | Section สำคัญ |
|---------------------|---------|---------------|
| **ภาพรวมโปรเจค** | [00-roadmap-overview.md](00-roadmap-overview.md) | - แนวคิด Station-first<br>- ขั้นตอนการพัฒนา 8 STEPS |
| **ความต้องการทั้งหมด** | [01-requirement-extraction.md](01-requirement-extraction.md) | - 7 Requirement Groups |
| **Priority ของงาน** | [02-requirement-grouping-priority.md](02-requirement-grouping-priority.md) | - Level 1: Core (ต้องมี)<br>- Level 2: Demo Value<br>- Level 3: Future |
| **สถาปัตยกรรมระบบ** | [03-system-scope-architecture.md](03-system-scope-architecture.md) | - 5 System Components<br>- Data Flow<br>- Scope Control |
| **ผู้ใช้และ Use Case** | [04-use-case-diagram.md](04-use-case-diagram.md) | - 3 Actors<br>- 9 Use Cases<br>- Use Case Summary Table |
| **การไหลของข้อมูล** | [05-data-flow-diagram.md](05-data-flow-diagram.md) | - DFD Level 0 & 1<br>- 6 Processes<br>- 4 Data Stores |
| **โครงสร้างฐานข้อมูล** | [06-erd-database-design.md](06-erd-database-design.md) | - 7 Entities<br>- Relationships<br>- Constraints |
| **API และสิทธิ์** | [07-data-model-api-design.md](07-data-model-api-design.md) | - Data Model Details<br>- API Endpoints<br>- Access Control Matrix |
| **รายงานฉบับเต็ม** | [project-report.md](project-report.md) | - สรุปทุกส่วน |

---

### 🔑 Key Concepts (แนวคิดสำคัญ)

| Concept | คำอธิบาย | เอกสารอ้างอิง |
|---------|----------|---------------|
| **Station-first Architecture** | ระบบหมุนรอบ Station เป็นศูนย์กลาง | [03-system-scope-architecture.md](03-system-scope-architecture.md) |
| **Time-series Data** | ข้อมูล SensorData เก็บตามเวลา | [06-erd-database-design.md](06-erd-database-design.md#entity-5-sensordata) |
| **RBAC** | Role-Based Access Control | [07-data-model-api-design.md](07-data-model-api-design.md#role-based-access-control) |
| **Mock Data** | ใช้ Mock แทน Hardware จริง | [02-requirement-grouping-priority.md](02-requirement-grouping-priority.md) |
| **3 Priority Levels** | Core > Demo Value > Future | [02-requirement-grouping-priority.md](02-requirement-grouping-priority.md) |

---

### 👥 Actors & Roles

| Actor | สิทธิ์การใช้งาน | Use Cases |
|-------|----------------|-----------|
| **User** | ดูข้อมูลทั่วไป | Login, View Dashboard, View Sensor Data, Receive Notification |
| **Manager** | ดูข้อมูล + จัดการ Station/Sensor | + View Alert, Manage Station, Manage Sensor |
| **Super User** | สิทธิ์เต็ม | + Configure Threshold, Manage Users |

---

### 🗄️ Entities & Relationships

| Entity | Primary Key | Related To | Relationship |
|--------|-------------|------------|--------------|
| **Role** | role_id | User | 1:N |
| **User** | user_id | Role, Threshold | N:1, 1:N |
| **Station** | station_id | Sensor, Alert | 1:N, 1:N |
| **Sensor** | sensor_id | Station, SensorData, Alert | N:1, 1:N, 1:N |
| **SensorData** | data_id | Sensor | N:1 |
| **Alert** | alert_id | Station, Sensor | N:1, N:1 |
| **Threshold** | threshold_id | User | N:1 |

---

### 🌐 API Endpoints Quick Reference

| Method | Endpoint | Purpose | Access |
|--------|----------|---------|--------|
| POST | `/api/auth/login` | Login | Public |
| GET | `/api/stations` | List stations | All |
| GET | `/api/stations/{id}` | Station detail | All |
| POST | `/api/stations` | Create station | Manager, Super User |
| GET | `/api/sensors/{id}/data` | Get sensor data | All |
| POST | `/api/sensors/{id}/data` | Ingest data | System |
| GET | `/api/alerts` | List alerts | All |
| PUT | `/api/alerts/{id}/ack` | Acknowledge alert | All |
| GET | `/api/thresholds` | List thresholds | All |
| POST | `/api/thresholds` | Create threshold | Super User |
| GET | `/api/users` | List users | Super User |
| POST | `/api/users` | Create user | Super User |

---

### 🎨 UI Components & Pages

| Page/Component | Use Case | Priority | Status Colors |
|----------------|----------|----------|---------------|
| Login | UC01 | Level 1 | - |
| Dashboard | UC02 | Level 1 | Normal (green), Warning (yellow), Critical (red) |
| Station List | UC03 | Level 1 | Status badges |
| Station Detail | UC04 | Level 1 | Station header + Sensors grid |
| Sensor Detail | UC05 | Level 1 | Time-series chart |
| Alert List | UC06 | Level 2 | Severity badges (LOW/MEDIUM/HIGH) |
| User Management | UC09 | Level 1 | Admin only |
| Threshold Config | UC08 | Level 2 | Admin only |

---

## 🔧 Troubleshooting

### ❓ คำถามที่พบบ่อย (FAQ)

#### Q1: ถ้าต้องการเพิ่มฟีเจอร์ใหม่ที่ไม่มีในเอกสารต้องทำอย่างไร?

**A:** 
```
1. ตรวจสอบว่าฟีเจอร์นั้นสอดคล้องกับ Business Requirement หรือไม่
2. ถามผู้ดูแลโปรเจคก่อน
3. ถ้าอนุมัติ → อัพเดทเอกสารก่อน แล้วค่อยทำ
4. ห้ามสร้างโดยไม่มีการอนุมัติ
```

---

#### Q2: ถ้าเอกสารขัดแย้งกันต้องทำอย่างไร?

**A:**
```
ลำดับความสำคัญของเอกสาร:
1. ERD (06-erd-database-design.md) - ที่สุด
2. Data Model & API (07-data-model-api-design.md)
3. Use Case (04-use-case-diagram.md)
4. DFD (05-data-flow-diagram.md)
5. Architecture (03-system-scope-architecture.md)

ถ้ายังขัดแย้ง → ถามผู้ดูแลโปรเจค
```

---

#### Q3: ถ้าต้องการเปลี่ยน Data Model ต้องทำอย่างไร?

**A:**
```
❌ ห้ามเปลี่ยนโดยพลการ

ถ้าจำเป็นจริง ๆ:
1. เสนอเหตุผลชัดเจน
2. ประเมิน Impact (กระทบอะไรบ้าง)
3. รอการอนุมัติ
4. อัพเดทเอกสาร 06 + 07 ก่อน
5. แจ้งทีมทั้งหมด
6. ค่อยเปลี่ยน Implementation
```

---

#### Q4: Alert สีอะไรแทนอะไร?

**A:**
```
Severity Levels:
- LOW (สีเขียว/ฟ้า) → แจ้งเตือนเบา
- MEDIUM (สีเหลือง/ส้ม) → ต้องติดตาม
- HIGH (สีแดง) → ต้องดำเนินการทันที

Station Status:
- Normal (สีเขียว) → ปกติ
- Warning (สีเหลือง) → มี Alert บางส่วน
- Critical (สีแดง) → มี Alert ระดับ HIGH
```

---

#### Q5: ต้องใช้ Mock Data หรือ Real Data?

**A:**
```
Demo Scope:
✅ ใช้ Mock Data สำหรับ:
- Sensor Data (ข้อมูลจาก Sensor)
- Station Data (40 สถานี)
- Alert Generation

❌ ไม่ใช้ Real Hardware:
- ไม่ต้องเชื่อมต่อ Modbus จริง
- ไม่ต้องมี IoT Device จริง

อ้างอิง: 02-requirement-grouping-priority.md#priority-level-1
```

---

### 🐛 Common Mistakes & Solutions

| ❌ ข้อผิดพลาดที่พบบ่อย | ✅ วิธีแก้ไข |
|----------------------|-------------|
| สร้าง API ที่ไม่มีในเอกสาร | ตรวจสอบ 07-data-model-api-design.md ก่อน |
| ไม่ตรวจสอบ Role | เช็ค Access Control Matrix ทุกครั้ง |
| เพิ่ม Field ใน Database | ดู ERD ใน 06-erd-database-design.md ก่อน |
| ทำ Priority Level 3 | เช็ค 02-requirement-grouping-priority.md |
| ไม่ทำ Error Handling | อ่าน API Design Principles ใน 07 |
| ใช้ plain password | ต้องใช้ password_hash เท่านั้น |

---

## 📈 Best Practices

### ✨ การเขียนโค้ดที่ดี

1. **Type-safe**: ใช้ TypeScript อย่างเต็มที่
2. **Validation**: Validate Input ตาม Constraints
3. **Error Handling**: Handle ทุก Error Case
4. **Naming**: ใช้ชื่อตามที่กำหนดใน Data Model
5. **Comments**: อ้างอิง Use Case/Entity ใน comment

### 🎯 การทำงานที่มีประสิทธิภาพ

1. **อ่านเอกสารก่อนทุกครั้ง**
2. **ทำทีละ Feature** - อย่าทำหลาย Feature พร้อมกัน
3. **Validate บ่อย ๆ** - ตรวจสอบกับเอกสารเป็นระยะ
4. **ถามเมื่อสงสัย** - อย่าสมมติ
5. **Commit บ่อย** - แต่ Commit ที่มีความหมาย

---

## 🎓 การเรียนรู้โปรเจค (Learning Path)

### สำหรับคนใหม่ (Beginner)

```
Day 1: อ่านเอกสารพื้นฐาน
- 00-roadmap-overview.md
- 02-requirement-grouping-priority.md
- 04-use-case-diagram.md

Day 2: ทำความเข้าใจ Data Model
- 06-erd-database-design.md
- 07-data-model-api-design.md

Day 3: เริ่มพัฒนา
- เลือก Use Case ง่าย ๆ (เช่น UC01: Login)
- สร้างตามเอกสาร
- Validate กับเอกสาร

Day 4+: ค่อย ๆ ขยาย
- ทำ Use Case ที่ซับซ้อนขึ้น
- เรียนรู้จาก Code ที่มีอยู่
```

---

## 📞 ติดต่อ & ช่วยเหลือ

หากมีคำถามหรือพบปัญหา:

1. **ตรวจสอบเอกสารก่อน** - คำตอบอาจอยู่ในนี้
2. **ค้นหาใน Quick Reference Table**
3. **ดู Troubleshooting Section**
4. **ถามผู้ดูแลโปรเจค** - พร้อมอ้างอิงเอกสาร

---

## 🔄 การอัพเดทเอกสาร

เอกสารนี้จะถูกอัพเดทเมื่อ:
- มีการเปลี่ยนแปลง Requirement
- มีการเพิ่ม Use Case ใหม่
- มีการปรับปรุง Data Model
- มี Best Practice ใหม่

**หมายเหตุ:** ตรวจสอบวันที่อัพเดทล่าสุดที่ท้ายไฟล์เสมอ

---

**จัดทำโดย**: System Design Team  
**อัพเดทล่าสุด**: February 1, 2026  
**เวอร์ชัน**: 1.0  
**สถานะ**: Active - Ready to Use

---

> **💡 จำไว้เสมอ:**  
> **"AI Agent ที่ดี = อ่านเอกสารก่อนทำงาน + ถามเมื่อไม่แน่ใจ + ห้ามคิดเอง"**
