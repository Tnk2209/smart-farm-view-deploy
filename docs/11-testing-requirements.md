# STEP 11: Prototype Testing Requirements **New Update:2**

> **โจทย์การทดสอบระบบสำหรับงานนำเสนอ (20 คะแนน)**  
> การติดตั้งสถานีจริง 1 ชุด และทดสอบตามโจทย์ที่กำหนด

---

## 🎯 เป้าหมายของ STEP 11

แสดงให้กรรมการเห็นว่า:
1. ระบบทำงานได้จริง (not just mockup)
2. Hardware + Software เชื่อมต่อกันได้
3. รองรับสถานการณ์จริงในสนาม (offline, configurable)
4. คุณภาพและความน่าเชื่อถือของระบบ

**คะแนนจากการทดสอบ: 20 คะแนน (จาก 100)**

---

## 📦 สิ่งที่ต้องเตรียมสำหรับการทดสอบ **New Update:2**

### 1. Hardware (1 ชุดสมบูรณ์)

| Item | Specification | จำนวน |
|------|--------------|-------|
| **สถานีตรวจอากาศ** | รวม Sensor ครบ 6 ตัววัด | 1 ชุด |
| **IoT Gateway** | ARM Cortex A8, 512MB RAM, Linux | 1 เครื่อง |
| **Solar Panel** | 50W, Polycrystalline | 1 แผง |
| **Battery** | 12V, 35Ah | 1 ก้อน |
| **Control Cabinet** | IP55, พร้อม Digital Lock | 1 ตู้ |
| **Mounting Pole** | สูง 2 เมตร | 1 ต้น |

### 2. Software

- **Backend API** (deployed on cloud/local server)
- **Frontend Dashboard** (accessible via web browser)
- **Database** (PostgreSQL / MySQL / SQL Server)
- **Gateway Software** (Python script running on Gateway)

---

## 🧪 โจทย์การทดสอบทั้งหมด (3 โจทย์) **New Update:2**

---

## โจทย์ที่ 1: ทดสอบ Offline Mode **New Update:2**

### วัตถุประสงค์

พิสูจน์ว่าระบบสามารถเก็บข้อมูลไว้ได้เมื่อเน็ตหลุด และส่งข้อมูลย้อนหลังได้เมื่อกลับมา Online

### ขั้นตอนการทดสอบ

```
┌─────────────────────────────────────────────────────────────┐
│ Phase 1: Normal Operation (5 นาที)                         │
│ • Gateway ส่งข้อมูลปกติทุก 1 นาที                          │
│ • ตรวจสอบว่าข้อมูลเข้า Database                             │
│ • Dashboard แสดงข้อมูล Real-time                            │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ Phase 2: Simulate Network Failure (10 นาที)                │
│ • ตัดการเชื่อมต่ออินเทอร์เน็ต (ถอดสาย LAN / ปิด 4G)        │
│ • Gateway ยังคงเก็บข้อมูลใน Local Storage                   │
│ • แสดงข้อมูลใน Log file ของ Gateway                        │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ Phase 3: Restore Network (5 นาที)                          │
│ • เชื่อมต่ออินเทอร์เน็ตกลับมา                              │
│ • Gateway ตรวจพบว่ากลับมา Online                            │
│ • Gateway ส่งข้อมูลย้อนหลังทั้งหมด (10 records)            │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ Phase 4: Verification (ตรวจสอบผลลัพธ์)                     │
│ ✅ ข้อมูลครบทุก record (ไม่หาย)                            │
│ ✅ Timestamp ถูกต้องตามเวลาจริง                            │
│ ✅ Dashboard แสดงข้อมูลย้อนหลัง                             │
│ ✅ ไม่มี duplicate records                                 │
└─────────────────────────────────────────────────────────────┘
```

### เกณฑ์การผ่าน **New Update:2**

| Criteria | Expected Result |
|----------|----------------|
| **Data Loss** | 0% (ต้องไม่มีข้อมูลหาย) |
| **Timestamp Accuracy** | ต้องตรงกับเวลาที่วัดจริง |
| **Sync Time** | ภายใน 2 นาทีหลังกลับมา Online |
| **Duplicate Detection** | ไม่มี record ซ้ำ |

### Technical Implementation **New Update:2**

**Gateway Code (Python):**

```python
import sqlite3
import requests
import time
from datetime import datetime

class OfflineModeHandler:
    def __init__(self):
        self.local_db = sqlite3.connect('offline_buffer.db')
        self.create_buffer_table()
    
    def create_buffer_table(self):
        self.local_db.execute('''
            CREATE TABLE IF NOT EXISTS sensor_buffer (
                id INTEGER PRIMARY KEY AUTOINCREMENT,
                timestamp TEXT,
                data JSON,
                synced BOOLEAN DEFAULT 0
            )
        ''')
    
    def is_online(self):
        try:
            response = requests.get('https://api.example.com/health', timeout=5)
            return response.status_code == 200
        except:
            return False
    
    def save_locally(self, data):
        self.local_db.execute(
            'INSERT INTO sensor_buffer (timestamp, data) VALUES (?, ?)',
            (datetime.now().isoformat(), json.dumps(data))
        )
        self.local_db.commit()
    
    def sync_buffered_data(self):
        cursor = self.local_db.execute(
            'SELECT id, timestamp, data FROM sensor_buffer WHERE synced = 0'
        )
        
        for row in cursor:
            record_id, timestamp, data = row
            
            try:
                response = requests.post(
                    'https://api.example.com/telemetry',
                    json={'timestamp': timestamp, 'data': json.loads(data)}
                )
                
                if response.status_code == 200:
                    self.local_db.execute(
                        'UPDATE sensor_buffer SET synced = 1 WHERE id = ?',
                        (record_id,)
                    )
                    self.local_db.commit()
            except:
                pass  # จะ retry ในรอบถัดไป
```

---

## โจทย์ที่ 2: ทดสอบการปรับความถี่การส่งข้อมูล **New Update:2**

### วัตถุประสงค์

แสดงว่าสามารถปรับความถี่การส่งข้อมูลได้ผ่าน Web Config

### ขั้นตอนการทดสอบ

```
┌─────────────────────────────────────────────────────────────┐
│ Test Case 1: ตั้งค่าเริ่มต้น 5 นาที                        │
│ • Super User เข้า Web Config → Station Settings            │
│ • เลือก Frequency: 5 minutes                               │
│ • Click "Apply"                                             │
│ • รอ 15 นาที → ตรวจสอบว่าได้ข้อมูล 3 records             │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ Test Case 2: ปรับเป็น 1 นาที                               │
│ • เปลี่ยน Frequency: 1 minute                              │
│ • Click "Apply"                                             │
│ • รอ 5 นาที → ตรวจสอบว่าได้ข้อมูล 5 records              │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ Test Case 3: ปรับเป็น 10 นาที                              │
│ • เปลี่ยน Frequency: 10 minutes                            │
│ • Click "Apply"                                             │
│ • รอ 20 นาที → ตรวจสอบว่าได้ข้อมูล 2 records             │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ Verification (ตรวจสอบผลลัพธ์)                              │
│ ✅ ความถี่เปลี่ยนตาม Config                                 │
│ ✅ UI แสดง "Last Updated" ถูกต้อง                          │
│ ✅ ไม่มี data loss ระหว่างเปลี่ยน Frequency                │
└─────────────────────────────────────────────────────────────┘
```

### เกณฑ์การผ่าน **New Update:2**

| Frequency Setting | Expected Records (in 10 min) | Tolerance |
|-------------------|------------------------------|-----------|
| **1 minute** | 10 records | ±1 record |
| **5 minutes** | 2 records | exact |
| **10 minutes** | 1 record | exact |

### Technical Implementation **New Update:2**

**Web Config API:**

```typescript
// Backend API Endpoint
POST /api/stations/{id}/config

Request Body:
{
  "telemetry_frequency_minutes": 1 | 5 | 10
}

Response:
{
  "success": true,
  "message": "Configuration updated",
  "applied_at": "2026-02-13T10:30:00Z"
}
```

**Gateway Implementation:**

```python
import schedule
import configparser

class TelemetryScheduler:
    def __init__(self):
        self.current_frequency = 5  # default
        self.config_file = '/etc/gateway/config.ini'
    
    def read_config_from_server(self):
        try:
            response = requests.get('https://api.example.com/stations/1/config')
            data = response.json()
            new_frequency = data['telemetry_frequency_minutes']
            
            if new_frequency != self.current_frequency:
                self.update_schedule(new_frequency)
        except:
            pass  # ใช้ค่าเดิม
    
    def update_schedule(self, new_frequency):
        schedule.clear()  # ลบ schedule เดิม
        schedule.every(new_frequency).minutes.do(self.send_telemetry)
        self.current_frequency = new_frequency
        print(f"Frequency updated to {new_frequency} minutes")
    
    def send_telemetry(self):
        # อ่านค่าจาก sensors
        data = read_all_sensors()
        # ส่งไปยัง server
        send_to_server(data)
```

---

## โจทย์ที่ 3: ทดสอบการสั่งเปิด-ปิด Digital Lock **New Update:2**

### วัตถุประสงค์

แสดงว่า Super User สามารถสั่งเปิด-ปิดล็อคผ่าน Web Config ได้

### ขั้นตอนการทดสอบ

```
┌─────────────────────────────────────────────────────────────┐
│ Phase 1: Unlock Test                                       │
│ • Super User login                                          │
│ • เข้า Station Detail → Digital Lock Control               │
│ • สถานะปัจจุบัน: 🔒 LOCKED                                 │
│ • Click "Unlock Now"                                        │
│ • กรอก Reason: "Maintenance"                                │
│ • Click "Confirm"                                           │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ Phase 2: Verification                                       │
│ • ระบบส่ง Command ไปยัง Gateway                             │
│ • Gateway สั่งเปิดล็อคจริง (Physical Test)                  │
│ • กรรมการลองดึงประตู → เปิดได้                              │
│ • UI อัพเดตเป็น 🔓 UNLOCKED                                │
│ • บันทึก Audit Log: "Unlocked by Admin at 10:30"          │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ Phase 3: Auto-lock Test                                     │
│ • รอ 30 นาที (ตาม Auto-lock timeout)                       │
│ • ระบบล็อคอัตโนมัติ                                         │
│ • กรรมการลองดึงประตู → ล็อคแล้ว (เปิดไม่ได้)               │
│ • UI อัพเดตเป็น 🔒 LOCKED                                   │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ Phase 4: Manual Lock Test                                   │
│ • Super User click "Lock Now"                               │
│ • ระบบล็อคทันที                                             │
│ • UI อัพเดต + บันทึก Log                                    │
└─────────────────────────────────────────────────────────────┘
```

### เกณฑ์การผ่าน **New Update:2**

| Test | Expected Result |
|------|----------------|
| **Unlock Command** | ล็อคเปิดจริง (Physical) ภายใน 5 วินาที |
| **UI Update** | Status อัพเดตภายใน 10 วินาที |
| **Audit Log** | บันทึกครบ: User, Action, Timestamp, Reason |
| **Auto-lock** | ล็อคกลับหลังจากเวลาที่กำหนด (± 1 นาที) |

### Technical Implementation **New Update:2**

**Control Flow:**

```
Web UI (Super User)
    ↓ POST /api/stations/{id}/lock/unlock
Backend API
    ↓ Publish MQTT Message
MQTT Broker
    ↓ Subscribe
Gateway (Python)
    ↓ GPIO Control
Digital Lock (Relay)
    ↓ Physical Unlock
    ↓ Send Status Update
Backend API
    ↓ Update Database
    ↓ Notify Frontend (WebSocket)
Web UI
    ↓ Update Status Display
```

**Gateway Code:**

```python
import paho.mqtt.client as mqtt
import RPi.GPIO as GPIO

class DigitalLockController:
    LOCK_PIN = 17  # GPIO Pin สำหรับควบคุม Relay
    
    def __init__(self):
        GPIO.setmode(GPIO.BCM)
        GPIO.setup(self.LOCK_PIN, GPIO.OUT)
        self.setup_mqtt()
    
    def setup_mqtt(self):
        self.client = mqtt.Client()
        self.client.on_message = self.on_message
        self.client.connect('mqtt.example.com', 1883)
        self.client.subscribe('station/1/lock/command')
        self.client.loop_start()
    
    def on_message(self, client, userdata, msg):
        command = msg.payload.decode()
        
        if command == 'UNLOCK':
            self.unlock()
        elif command == 'LOCK':
            self.lock()
    
    def unlock(self):
        GPIO.output(self.LOCK_PIN, GPIO.HIGH)  # ส่งสัญญาณเปิด
        print("Digital Lock: UNLOCKED")
        self.send_status_update('UNLOCKED')
    
    def lock(self):
        GPIO.output(self.LOCK_PIN, GPIO.LOW)  # ส่งสัญญาณปิด
        print("Digital Lock: LOCKED")
        self.send_status_update('LOCKED')
    
    def send_status_update(self, status):
        requests.post('https://api.example.com/stations/1/lock/status', json={
            'status': status,
            'timestamp': datetime.now().isoformat()
        })
```

---

## 📋 Test Checklist (สำหรับการนำเสนอ) **New Update:2**

### ก่อนวันนำเสนอ

- [ ] ติดตั้ง Hardware ครบ 1 ชุด
- [ ] Gateway ส่งข้อมูลได้ปกติ
- [ ] Dashboard แสดงข้อมูล Real-time
- [ ] ทดสอบ Offline Mode ล่วงหน้า
- [ ] ทดสอบการปรับ Frequency ล่วงหน้า
- [ ] ทดสอบ Digital Lock ล่วงหน้า
- [ ] เตรียมสลิดอธิบายการทดสอบ
- [ ] เตรียม Video บันทึกการทดสอบ (backup)

### ในวันนำเสนอ

- [ ] เช็คการเชื่อมต่ออินเทอร์เน็ต
- [ ] เช็คแบตเตอรี่และ Solar Panel
- [ ] เช็ค Digital Lock ทำงานปกติ
- [ ] เตรียม Timeline สำหรับแต่ละโจทย์
- [ ] มี backup plan ถ้า hardware มีปัญหา

---

## 📊 การบันทึกผลการทดสอบ **New Update:2**

### Test Report Template

```markdown
# Prototype Testing Report
Date: 2026-02-13
Location: [ระบุสถานที่ทดสอบ]
Tested By: [ชื่อทีม]

## Test 1: Offline Mode
- Start Time: 10:00
- Network Down Period: 10:05 - 10:15 (10 minutes)
- Network Restored: 10:15
- Records Collected During Offline: 10 records
- Records Synced: 10 records
- Data Loss: 0%
- Result: ✅ PASS

## Test 2: Frequency Configuration
- Test Case 1 (5 min): ✅ PASS
- Test Case 2 (1 min): ✅ PASS
- Test Case 3 (10 min): ✅ PASS

## Test 3: Digital Lock Control
- Unlock Command Response Time: 3 seconds
- Physical Lock Status: ✅ Unlocked
- UI Update Time: 8 seconds
- Auto-lock After 30 min: ✅ PASS
- Audit Log: ✅ Complete
```

---

## 🎬 Video Recording Requirements **New Update:2**

### บันทึกวิดีโอแสดง

1. **Overview Shot** (1 นาที)
   - แสดง Hardware ทั้งชุด
   - แสดง Dashboard บน Browser

2. **Test 1: Offline Mode** (3 นาที)
   - แสดงการตัดเน็ต
   - แสดง Log บน Gateway
   - แสดงการกลับมา Online
   - แสดงข้อมูลใน Dashboard

3. **Test 2: Frequency Config** (2 นาที)
   - แสดงหน้า Web Config
   - เปลี่ยน Frequency
   - แสดงข้อมูลเข้าตาม Frequency ใหม่

4. **Test 3: Digital Lock** (2 นาที)
   - แสดงการสั่ง Unlock
   - ลองดึงประตู (เปิดได้)
   - แสดง Auto-lock
   - ลองดึงประตู (เปิดไม่ได้)

**รวม: 8-10 นาที**

---

## 📌 หมายเหตุสำหรับ AI Agent

**เมื่อเตรียมการทดสอบ:**

1. สร้าง Test Plan ที่ละเอียด
2. เขียน Test Script สำหรับแต่ละโจทย์
3. เตรียม Mock Data สำหรับ Demo
4. ทดสอบล่วงหน้าหลายครั้ง
5. เตรียม Backup Plan

**ใน Demo Phase:**

- ถ้าไม่มี Hardware จริง → ใช้ Simulation
- แสดง Flow Diagram อธิบายการทดสอบ
- เตรียม Video บันทึกผลการทดสอบ
- อธิบายวิธีการทดสอบอย่างละเอียด

**ห้าม:**

- นำเสนอโดยไม่เตรียมทดสอบล่วงหน้า
- ใช้ Mock Data เท่านั้นโดยไม่มี Hardware
- ไม่มี Backup Plan

---

**สิ้นสุด STEP 11**

---

**หมายเหตุ:**  
การทดสอบ Prototype คือส่วนที่แยกความแตกต่างระหว่าง "แค่ออกแบบ" กับ "ทำได้จริง"  
ใช้เวลาเตรียมตัวให้ดี และทดสอบซ้ำหลายครั้งก่อนนำเสนอ

**New Update:2 (13/02/2026)**
