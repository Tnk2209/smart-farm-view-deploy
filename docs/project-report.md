# รายงานการออกแบบระบบติดตามการเกษตรอัจฉริยะ

> **Smart Agriculture Monitoring System**  
> รายงานการวิเคราะห์ TOR และการวางแผนพัฒนาโครงการ

---

## 1. บทนำ (Introduction)

รายงานฉบับนี้จัดทำขึ้นเพื่อนำเสนอ**แผนการดำเนินงาน (Roadmap)** และ**การออกแบบเชิงเทคนิค (System Design)** สำหรับโครงการ Smart Agriculture Monitoring System โดยมีวัตถุประสงค์เพื่อพัฒนาระบบติดตามข้อมูลทางการเกษตรจากอุปกรณ์ IoT ในรูปแบบ Dashboard ที่แสดงผลแบบ Real-time และสามารถแจ้งเตือนเมื่อเกิดความผิดปกติได้

โครงสร้างของรายงานอ้างอิงตามขั้นตอนการพัฒนาซอฟต์แวร์ (SDLC) ตั้งแต่การวิเคราะห์ความต้องการ ไปจนถึงการออกแบบโครงสร้างฐานข้อมูลและ API

---

## 2. การวิเคราะห์ความต้องการของระบบ (Requirement Analysis)

จากการวิเคราะห์ขอบเขตงาน (TOR) สามารถจำแนกความต้องการออกเป็นกลุ่มต่าง ๆ และจัดลำดับความสำคัญได้ดังนี้

### 2.1 กลุ่มความต้องการหลัก (Requirement Groups)

#### 1. ผู้ใช้งานและสิทธิ์ (User & Role)

- ระบบต้องรองรับผู้ใช้งานหลายระดับ (Manager, Super User, User)
- มีการกำหนดสิทธิ์การเข้าถึงข้อมูลที่แตกต่างกัน
- มีระบบบันทึกการใช้งาน (Audit Log)

#### 2. แหล่งข้อมูลและอุปกรณ์ IoT (Data Source & IoT)

- รองรับการรับข้อมูลจาก Sensor และ IoT Gateway (Modbus TCP/RTU)
- ข้อมูลมีลักษณะเป็น Time-series data (ข้อมูลต่อเนื่องตามเวลา)

#### 3. การจัดการข้อมูลและฐานข้อมูล (Data Management)

- ออกแบบเพื่อรองรับข้อมูลปริมาณมาก (High Volume)
- มีการสำรองข้อมูล (Backup) และรองรับความปลอดภัยระดับ Field-level

#### 4. เว็บแอปพลิเคชันและแดชบอร์ด (Web Application & Dashboard)

- แสดงผลผ่าน Web Application รองรับ Responsive
- แสดงข้อมูลแบบ Real-time / Near real-time พร้อมกราฟ Visualization

#### 5. การแจ้งเตือน (Alert & Notification)

- แจ้งเตือนเมื่อค่า Sensor เกินกำหนด (Threshold)
- แบ่งระดับความรุนแรงของการแจ้งเตือน (Severity Levels)

#### 6. อัลกอริทึมและการวิเคราะห์ (Algorithm & Analysis)

- มีการคำนวณค่าทางอุตุนิยมวิทยาหรือสูตรการเกษตร (เช่น LWD, MAE) เพื่อประเมินสถานการณ์

#### 7. การเชื่อมต่อระบบภายนอก (API & Integration)

- ให้บริการข้อมูลผ่าน RESTful API ที่เป็นมาตรฐาน

---

### 2.2 การจัดลำดับความสำคัญ (Requirement Priority)

เพื่อให้การพัฒนาระบบต้นแบบ (Demo) และระบบจริงสอดคล้องกัน จึงแบ่งลำดับความสำคัญดังนี้:

- **ระดับ 1: Core Requirements** (ต้องมีใน Demo)
  - ระบบจัดการผู้ใช้ (User & Role)
  - การนำเข้าข้อมูล (Data Ingestion)
  - การออกแบบฐานข้อมูล (Database)
  - Dashboard พื้นฐาน

- **ระดับ 2: Demo Value** (ควรมีเพื่อความสมบูรณ์)
  - ระบบแจ้งเตือน (Alert) แบบ Simple Rule
  - อัลกอริทึมจำลอง (Mock Logic) เพื่อแสดงผลลัพธ์

- **ระดับ 3: Future Phase** (ส่วนขยายในอนาคต)
  - ระบบ High Availability (HA)
  - Security ขั้นสูง
  - การเชื่อมต่อ Hardware จริง (ใน Demo จะใช้ Mock Data)

---

## 3. สถาปัตยกรรมระบบ (System Architecture)

ระบบถูกออกแบบภายใต้แนวคิด **"Station-first Architecture"** โดยให้ความสำคัญกับสถานีเกษตรเป็นศูนย์กลาง ข้อมูลจะถูกรวบรวมจาก Sensor ภายใต้สถานีนั้น ๆ

### 3.1 องค์ประกอบหลักของระบบ (System Components)

#### 1. Data Source Layer

**แหล่งกำเนิดข้อมูล** (Sensor/IoT Gateway)

ในระยะ Demo จะใช้ Mock Data Generator จำลองสถานีประมาณ 40 แห่ง

#### 2. Data Ingestion / API Layer

ส่วนรับข้อมูลผ่าน RESTful API ทำหน้าที่ Validate ข้อมูลเบื้องต้นก่อนส่งเข้าฐานข้อมูล

#### 3. Database Layer

ฐานข้อมูลสำหรับเก็บ Time-series data, Station info และ User data

#### 4. Processing & Logic Layer

ส่วนประมวลผล ตรวจสอบเงื่อนไข (Threshold) และสร้าง Alert ตาม Business Logic

#### 5. Web Application Layer

ส่วนแสดงผลสำหรับผู้ใช้ (Dashboard, Maps, Graphs)

---

### 3.2 การไหลของข้อมูล (Data Flow Overview)

```
Mock Sensor Data (per Station)
    ↓
API รับข้อมูล
    ↓
Database
    ↓
Processing Logic
    ↓
Dashboard / Alert
    ↓
User
```

---

## 4. การออกแบบการใช้งานระบบ (Use Case Design)

### 4.1 ผู้ใช้งานระบบ (Actors)

#### 1. Manager

**ผู้บริหาร** เน้นดูภาพรวมสถานการณ์ (Dashboard, Station Overview) เพื่อตัดสินใจ

#### 2. User

**เจ้าหน้าที่หน้างาน** เน้นดูรายละเอียดเชิงลึก (Sensor Data) และรับการแจ้งเตือน

#### 3. Super User

**ผู้ดูแลระบบ** เน้นการจัดการ (Configure Threshold, Manage Users)

---

### 4.2 Use Case หลัก

- **Login**: เข้าสู่ระบบและระบุ Role
- **View Dashboard**: ดูภาพรวมระดับประเทศและ Map-based visualization
- **View Station Overview**: ดูสถานะรวมของสถานี (Normal/Warning/Critical)
- **View Station Detail**: (Drill-down) ดูรายละเอียด Sensor ในสถานี
- **View Sensor Data**: ดูกราฟและประวัติข้อมูล
- **View Alert**: ดูรายการแจ้งเตือน
- **Manage Users**: (Super User) จัดการผู้ใช้
- **Configure Threshold**: (Super User) ตั้งค่าเกณฑ์การวัด
- **Register Farm Plot**: (User) ลงทะเบียนแปลงนา (ระบุ Lat/Lon เอง) **New Update:2**
- **Control Digital Lock**: (Super User) สั่งเปิด-ปิดกลอนประตู **New Update:2**
- **Generate Report**: (Manager) ออกรายงานสรุป **New Update:2**
- **Request Support**: (User) แจ้งซ่อมผ่าน QR Code **New Update:2**

---

## 5. การออกแบบการไหลของข้อมูล (Data Flow Diagram - DFD)

### 5.1 DFD Level 0 (Context Diagram)

ระบบ Smart Agriculture Monitoring System ทำงานเป็นศูนย์กลาง โดยรับข้อมูลจาก Sensor/IoT Devices (Input) และให้บริการข้อมูลแก่ User/Manager/Super User (Output/Interaction)

---

### 5.2 DFD Level 1 (Process Decomposition)

ในระดับ Level 1 ระบบจะถูกแตกออกเป็น Process ย่อย และมีการระบุ Data Store (แหล่งเก็บข้อมูล) อย่างชัดเจน ดังนี้

#### Processes (กระบวนการทำงานหลัก)

1. **P1 Authenticate User**: ตรวจสอบสิทธิ์การใช้งาน (Login)
2. **P2 Ingest Sensor Data**: รับข้อมูลจาก Sensor หรือ Mock Data
3. **P3 Store & Manage Data**: บริหารจัดการการบันทึกข้อมูลลงใน Data Store ที่เกี่ยวข้อง
4. **P4 Analyze & Check Threshold**: วิเคราะห์ค่าที่รับเข้ามาเปรียบเทียบกับค่าวิกฤต (Threshold) เพื่อสร้าง Alert
5. **P5 Display Dashboard & Alert**: ดึงข้อมูลจาก Data Store เพื่อแสดงผลให้ผู้ใช้
6. **P6 Manage Station**: จัดการข้อมูล Metadata ของสถานี (สร้าง/แก้ไข/ลบ)
7. **P7 Generate Reports**: สร้างรายงานสรุป (PDF/Excel) **New Update:2**
8. **P8 Manage Support & Media**: จัดการ Ticket และสื่อการเรียนรู้ **New Update:2**
9. **P9 Farm Registration**: ระบบลงทะเบียนแปลงนาแบบ Manual Input **New Update:2**

#### Data Stores (แหล่งเก็บข้อมูล)

ระบบประกอบด้วย 4 แหล่งข้อมูลหลัก (D1-D4):

- **D1: User Database** - เก็บข้อมูลผู้ใช้งาน (Username, Password Hash, Email) และบทบาท (Role)
- **D2: Sensor Data Database** - เก็บข้อมูลการวัดค่าจาก Sensor ในรูปแบบ Time-series
- **D3: Alert / Notification Data** - เก็บประวัติการแจ้งเตือนที่ระบบสร้างขึ้น
- **D4: Station Database** - เก็บข้อมูลรายละเอียดสถานีและความสัมพันธ์กับ Sensor

---

## 6. การออกแบบฐานข้อมูล (Database Design - ERD)

การออกแบบ Entity Relationship Diagram (ERD) ยึดตามหลักการ Normalization และรองรับ Time-series Data

### 6.1 รายชื่อ Entity หลัก

1. **Station**: สถานีเกษตร (Core Entity)
2. **Sensor**: อุปกรณ์ตรวจวัด (ติดตั้งอยู่ที่ Station)
3. **SensorData**: ข้อมูลค่าการวัดตามเวลา (Time-series)
4. **Alert**: การแจ้งเตือนความผิดปกติ
5. **User**: ผู้ใช้งานระบบ
6. **Role**: สิทธิ์การใช้งาน
7. **Threshold**: เกณฑ์การวัดค่าปกติ/ผิดปกติ
8. **FarmPlot**: แปลงนาเกษตรกร (GIS) **New Update:2**
9. **AuditLog**: ประวัติการใช้งาน **New Update:2**
10. **SupportTicket**: การแจ้งซ่อม **New Update:2**
11. **MediaContent**: สื่อการเรียนรู้ **New Update:2**

---

### 6.2 ความสัมพันธ์ (Relationships)

- **Station** 1:N **Sensor** (หนึ่งสถานีมีหลายเซนเซอร์)
- **Sensor** 1:N **SensorData** (หนึ่งเซนเซอร์มีข้อมูลวัดค่าได้หลายครั้ง)
- **Sensor** 1:N **Alert** (เซนเซอร์หนึ่งตัวสร้าง Alert ได้หลายครั้ง)
- **Station** 1:N **Alert** (Alert สามารถระบุที่มาได้ถึงระดับสถานี)
- **Role** 1:N **User** (หนึ่งบทบาทมีผู้ใช้ได้หลายคน)

---

## 7. รายละเอียด Data Model และ API Specification

ในส่วนนี้เป็นการแปลง ERD สู่ Data Model จริงสำหรับการพัฒนา Backend และระบุ API Endpoint ที่เกี่ยวข้อง

### 7.1 Data Model: ROLE & USER

ใช้สำหรับ **Authentication และ Authorization (RBAC)**

**Entities:** ROLE, USER

**Key Fields:** username, password_hash, role_id

**API Endpoints:**
- `POST /api/auth/login`
- `GET /api/users`
- `POST /api/users` (Super User Only)

---

### 7.2 Data Model: STATION (Core)

ตัวแทนของพื้นที่การเกษตร

**Fields:** station_id (PK), station_name, province, lat, long, status

**API Endpoints:**
- `GET /api/stations` (List all)
- `GET /api/stations/{id}` (Detail)
- `POST /api/stations` (Create - Manager/Admin)

---

### 7.3 Data Model: SENSOR & SENSOR_DATA (Time-series)

ส่วนที่เก็บข้อมูลที่มีปริมาณมากที่สุด (High Volume)

**Entities:** SENSOR, SENSOR_DATA

**Sensor Fields:** sensor_id, station_id, sensor_type, status

**Data Fields:** data_id, sensor_id, value, recorded_at

**Indexing:** จำเป็นต้องทำ Index ที่ sensor_id และ recorded_at เพื่อประสิทธิภาพในการ Query กราฟ

**API Endpoints:**
- `GET /api/sensors/{id}/data?from=...&to=...` (สำหรับพลอตกราฟ)
- `POST /api/sensors/{id}/data` (สำหรับ Ingest ข้อมูล)

---

### 7.4 Data Model: ALERT & THRESHOLD

ส่วนประมวลผลความฉลาดของระบบ

**Entities:** ALERT, THRESHOLD

**Threshold Constraints:** min_value < max_value, Unique per sensor type

**Alert Fields:** severity (LOW, MEDIUM, HIGH), is_acknowledged

**API Endpoints:**
- `GET /api/alerts`
- `PUT /api/alerts/{id}/ack` (รับทราบการแจ้งเตือน)
- `GET /api/thresholds`

---

### 7.5 ตารางสรุปสิทธิ์การใช้งาน (Access Control Matrix)

| Function / Feature | User | Manager | Super User |
|--------------------|------|---------|------------|
| View Dashboard | ✅ | ✅ | ✅ |
| View Sensor Data | ✅ | ✅ | ✅ |
| Manage Station | ❌ | ✅ | ✅ |
| Manage Sensor | ❌ | ✅ | ✅ |
| Configure Threshold | ❌ | ❌ | ✅ |
| Manage User | ❌ | ❌ | ✅ |
| Register Plot | ✅ | ❌ | ❌ | **New Update:2**
| Control Lock | ❌ | ❌ | ✅ | **New Update:2**
| Generate Report | ❌ | ✅ | ✅ | **New Update:2**

---

## 8. บทสรุป (Conclusion)

การออกแบบระบบ Smart Agriculture Monitoring System ในครั้งนี้ มุ่งเน้นความยืดหยุ่นในการขยายตัว (Scalability) ด้วยสถาปัตยกรรมแบบ **Station-first** และการแยก Layer ของข้อมูลชัดเจน

โครงสร้าง Database และ API ที่ออกแบบไว้ รองรับทั้งการทำ Demo เพื่อแสดงวิสัยทัศน์ และพร้อมสำหรับการต่อยอดเป็น Production จริง โดยรองรับ Time-series data, การจัดการสิทธิ์ผู้ใช้ และระบบแจ้งเตือนที่ครบถ้วนตามความต้องการของโครงการ

---

## 📚 เอกสารอ้างอิง

สำหรับรายละเอียดเพิ่มเติมแต่ละหัวข้อ กรุณาดูเอกสารแยกตาม STEP:

- [00-roadmap-overview.md](00-roadmap-overview.md) - Roadmap ทั้งโปรเจค
- [01-requirement-extraction.md](01-requirement-extraction.md) - การสกัดความต้องการ
- [02-requirement-grouping-priority.md](02-requirement-grouping-priority.md) - การจัดลำดับความสำคัญ
- [03-system-scope-architecture.md](03-system-scope-architecture.md) - สถาปัตยกรรมระบบ
- [04-use-case-diagram.md](04-use-case-diagram.md) - Use Case Diagram
- [05-data-flow-diagram.md](05-data-flow-diagram.md) - Data Flow Diagram
- [06-erd-database-design.md](06-erd-database-design.md) - ERD และ Database Design
- [07-data-model-api-design.md](07-data-model-api-design.md) - Data Model และ API Design

---

**จัดทำโดย**: System Design Team  
**วันที่**: February 1, 2026  
**เวอร์ชัน**: 2.0  
**สถานะ**: Design Complete, Ready for Implementation

**New Update:2 (13/02/2026)**
